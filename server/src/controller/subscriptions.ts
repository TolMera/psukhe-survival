/**
 * A way of subscribing sockets to event generators
 */
export default class Subscriptions {
    constructor() {
        console.log("Subscription service running");
    }

    subscribe(socket, room: number) {

    }

    unsubscribe(socket, room: number) {
        
    }
}